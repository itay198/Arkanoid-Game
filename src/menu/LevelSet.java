package menu;

import biuoop.KeyboardSensor;

/**
 * The type Level set.
 */
public class LevelSet extends MenuAnimation {

    /**
     * Instantiates a new Menu animation.
     *
     * @param title          the title
     * @param keyboardSensor the keyboard sensor
     */
    public LevelSet(String title, KeyboardSensor keyboardSensor) {
        super(title, keyboardSensor);
    }
}